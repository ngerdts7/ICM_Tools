# Overview
This example script shows how two scenarios can be comapared.

The script references two scenarios in the provided "PurpleTown" model. The "Break" scenario adds a simple burst of demand on a junction at 10:30 to represent a pipe break.
Be sure to extract and re-run both scenarios before trying to run the scripts.
